{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Meta-Parameters for GCN for Round14",
    "technique": "GCN",
    "technique_description": "GCN Pipeline to Detect Trojaned Models",
    "technique_changes": "Round14-Run1 (rl-lavaworld-jul2023)",
    "technique_type": ["Other"],
    "commit_id": "11b59fe",
    "repo_name": "https://github.com/UMBC-Coral-Lab/trojai-submission-all/tree/round14-rl-lavaworld-jul2023-gcn",
    "required": [ 
        "train_epochs", 
        "train_batch_size", 
        "train_feature_vector" 
    ],
    "additionalProperties": false,
    "type": "object",
    "properties": {
        "train_epochs": {
            "description": "The number of epochs to train the classifier for.",
            "type": "integer",
            "minimum": 20,
            "maximum": 41,
            "suggested_minimum": 30,
            "suggested_maximum": 35
        },
        "train_batch_size": {
            "description": "The batch size to use to train the classifier.",
            "type": "integer",
            "minimum": 18,
            "maximum": 37,
            "suggested_minimum": 18,
            "suggested_maximum": 24
        },
        "train_feature_vector": {
            "description": "The training node features vector to use.",
            "type": "integer",
            "minimum": 4,
            "maximum": 12,
            "suggested_minimum": 6,
            "suggested_maximum": 9
        }
    }
}
